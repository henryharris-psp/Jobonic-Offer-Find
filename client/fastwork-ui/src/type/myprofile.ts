type EducationInstance = {
    id: string;
    profileId: string;
    institute: string;
    degree: string;
    startDate: string;
    endDate: string;
};

type ExperienceInstance = {
    id: string;
    profileId: string;
    company: string;
    startDate: string;
    endDate: string;
};

type SkillInstance = {
    id: string;
    name: string;
};